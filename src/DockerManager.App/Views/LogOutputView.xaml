<UserControl x:Class="DockerManager.App.Views.LogOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DockerManager.App.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:LogLevelToBrushConverter x:Key="LogLevelBrush"/>
    </UserControl.Resources>

    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="5,3">
            <TextBlock Text="Log Output" FontWeight="Bold" Foreground="White"
                       VerticalAlignment="Center"/>
            <Button Content="Pulisci" Command="{Binding ClearCommand}"
                    HorizontalAlignment="Right" Padding="8,2"/>
        </DockPanel>

        <ListBox x:Name="LogList"
                 ItemsSource="{Binding LogEntries}"
                 Background="#0C0C0C"
                 Foreground="White"
                 BorderThickness="0"
                 FontFamily="Consolas"
                 FontSize="12"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 VirtualizingPanel.ScrollUnit="Pixel">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               Foreground="{Binding Level, Converter={StaticResource LogLevelBrush}}"
                               TextWrapping="NoWrap"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
