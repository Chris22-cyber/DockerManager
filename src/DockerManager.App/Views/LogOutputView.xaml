<UserControl x:Class="DockerManager.App.Views.LogOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DockerManager.App.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:LogLevelToBrushConverter x:Key="LogLevelBrush"/>
    </UserControl.Resources>

    <DockPanel Background="{DynamicResource SurfaceContainerLow}">
        <DockPanel DockPanel.Dock="Top" Margin="12,6">
            <TextBlock Text="Log Output" FontFamily="Segoe UI Semibold" FontSize="12"
                       Foreground="{DynamicResource OnSurfaceVariant}" VerticalAlignment="Center"/>
            <Button Content="Pulisci" Command="{Binding ClearCommand}"
                    Style="{StaticResource TertiaryButton}"
                    HorizontalAlignment="Right" Padding="8,3"/>
        </DockPanel>

        <Border Margin="8,0,8,8" Background="{DynamicResource SurfaceLowest}"
                CornerRadius="8" ClipToBounds="True">
            <ListBox x:Name="LogList"
                     ItemsSource="{Binding LogEntries}"
                     Background="Transparent"
                     BorderThickness="0"
                     FontFamily="Cascadia Code, Consolas, Courier New"
                     FontSize="12"
                     Padding="8,4"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.ScrollUnit="Pixel">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="4,1"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   Foreground="{Binding Level, Converter={StaticResource LogLevelBrush}}"
                                   TextWrapping="Wrap"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </DockPanel>
</UserControl>
