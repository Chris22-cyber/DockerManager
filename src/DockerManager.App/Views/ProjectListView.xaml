<UserControl x:Class="DockerManager.App.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Margin="5">
            <Button Content="+ Nuovo Progetto"
                    Command="{Binding AddProjectCommand}"
                    Padding="8,4" Margin="0,5,0,0"/>
            <Button Content="Rimuovi"
                    Command="{Binding RemoveProjectCommand}"
                    Padding="8,4" Margin="0,5,0,0"
                    Foreground="Red"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding Projects}"
                 SelectedItem="{Binding SelectedProject}"
                 Margin="5"
                 Background="#1E1E1E"
                 Foreground="White"
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13"/>
                        <TextBlock Text="{Binding ImageName}" FontSize="11" Opacity="0.7"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
